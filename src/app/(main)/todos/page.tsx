"use client";

// 라우트 보호((main) 레이아웃)를 검증하려면 대상 페이지가 존재해야 한다. 내용은 Phase 8에서 구현한다.
export default function TodosPage() {
  return (
    <div className="flex flex-col gap-2">
      <h1 className="text-lg font-semibold">할 일 목록</h1>
      <p className="text-sm text-muted-foreground">Phase 8에서 구현합니다.</p>
    </div>
  );
}
